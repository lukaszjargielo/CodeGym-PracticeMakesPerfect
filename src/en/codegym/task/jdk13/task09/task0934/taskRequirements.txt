
Implement the isPalindrome(String) method so that it checks whether the input word is a palindrome.
A palindrome is a word that read the same in both directions.

Don't consider whether the letters are uppercase or lowercase.

Requirements:
•	The isPalindrome(String) method needs to return true if the passed word is a palindrome and false if it is not a palindrome

package en.codegym.task.jdk13.task09.task0934;

/* 
Word palindrome
*/

public class Solution {
    public static void main(String[] args) {
        String word = "Rotor";
        String answer = isPalindrome(word) ? "Yes" : "No";
        System.out.println("Is the word \"" + word + "\" a palindrome? " + answer);
    }

    public static boolean isPalindrome(String word) {
        //write your code here
        return false;
    }
}