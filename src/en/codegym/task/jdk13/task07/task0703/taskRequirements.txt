
Create an array of 20 numbers.
Populate it with values from the keyboard.
Create two arrays of 10 numbers each.
Copy the large array into the two small ones: half the numbers into the first small array, and the second half into the second small array.
Display the second small array, each value on a new line.
Requirements:
•	The program must create a large array and initialize it with a new int[20] value.
•	The program should read 20 numbers from the keyboard for the large array.
•	The program must create two small arrays and initialize them with a new int[10] value.
•	The program must copy one half of the large array into the first small array, copy the second half into the second array, and then display the second small array on the screen.

package en.codegym.task.jdk13.task07.task0705;

import java.io.BufferedReader;
import java.io.InputStreamReader;

/* 
One large array and two small ones
*/

public class Solution {
    public static void main(String[] args) throws Exception {
        //write your code here
    }
}
